apiVersion: v1
kind: Service
metadata:
  name: tetris-service
  labels:
    app: tetris
spec:
  type: NodePort
  selector:
    app: tetris
    version: blue  
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
